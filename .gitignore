docs/
.fackr/
build/
ferp
*.mod
CLAUDE.md

# Regenerated by tests/extended_grep_test.sh on every run. It is an absolute
# symlink, so a tracked copy points at whatever checkout created it.
tests/extended_fixtures/recursive/link_to_basic.txt